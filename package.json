{
  "name": "preact-global",
  "version": "0.0.1",
  "description": "Auto-refreshing global component state for preact",
  "scripts": {
    "build": "babel src/index.js > index.js",
    "prepublish": "npm run build",
    "deploy": "cd example && npm run build && gh-pages -d build"
  },
  "keywords": [
    "preact",
    "state",
    "context"
  ],
  "author": "David Konsumer <konsumer@jetboystudio.com> (http://konsumer.js.org)",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-react-jsx": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1"
  },
  "dependencies": {
    "babel-plugin-transform-decorators-legacy": "^1.3.5"
  },
  "files": [
    "index.js"
  ]
}
